# VS-31 Rendered Verification

Rendered acceptance is automated by `apps/web/e2e/operator-shell-navigation.spec.ts`.

The exact-head review must verify:

- shared NoorPath wordmark/header/sidebar on representative operator collection routes;
- legacy package authoring embedded without a competing operator sidebar/brand;
- active route indication;
- 390px mobile navigation disclosure and no horizontal overflow;
- signed-in member identity remains separate from the operator business name.

Exact-head Rendered Slice Review remains authoritative for final acceptance.
