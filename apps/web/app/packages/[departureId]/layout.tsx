import type { ReactNode } from "react";
import "./package-conversion-accessibility.css";
import "./package-conversion-reflow.css";

export default function PackageDepartureLayout({
  children,
}: Readonly<{ children: ReactNode }>) {
  return children;
}
