{
  "name": "noorpath",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm --recursive build",
    "check": "pnpm --recursive check",
    "format": "pnpm --dir apps/web exec prettier --write \"../../package.json\" \"../../delivery/**/*.json\" \"../../scripts/**/*.mjs\" \"../../.github/workflows/*.yml\" && pnpm --recursive format",
    "format:check": "pnpm --dir apps/web exec prettier --check \"../../package.json\" \"../../delivery/**/*.json\" \"../../scripts/**/*.mjs\" \"../../.github/workflows/*.yml\" && pnpm --recursive format:check",
    "hooks:install": "node scripts/install-git-hooks.mjs",
    "migrations:registry": "node scripts/module-migrations.mjs validate-registry",
    "prepare": "node scripts/install-git-hooks.mjs",
    "preflight": "pnpm slice:validate && pnpm migrations:registry && pnpm format:check && pnpm check && dotnet build NoorPath.slnx --configuration Release",
    "slice:new": "node scripts/slice.mjs new",
    "slice:resolve": "node scripts/slice.mjs resolve",
    "slice:status": "node scripts/slice.mjs status",
    "slice:validate": "node scripts/slice.mjs validate",
    "test": "pnpm --recursive test",
    "migrations:model": "node scripts/module-migrations.mjs validate-model",
    "certify": "pnpm slice:validate && pnpm migrations:registry && pnpm format:check && pnpm check && pnpm test && pnpm build && dotnet restore NoorPath.slnx && dotnet format NoorPath.slnx --verify-no-changes --no-restore && dotnet build NoorPath.slnx --configuration Release --no-restore && pnpm migrations:model",
    "precert": "pnpm certify"
  }
}
