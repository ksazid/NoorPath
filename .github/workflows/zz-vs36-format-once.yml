name: VS-36 One-shot Formatter

on:
  push:
    branches:
      - agent/vs36-package-details-design-correction

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/vs36-package-details-design-correction
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - name: Format VS-36 frontend files
        run: >-
          pnpm --dir apps/web exec prettier --write
          'app/packages/[departureId]/PackageContentIcon.tsx'
          'app/packages/[departureId]/page.tsx'
          'e2e/package-details.spec.ts'
      - name: Commit formatted files
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add 'apps/web/app/packages/[departureId]/PackageContentIcon.tsx' 'apps/web/app/packages/[departureId]/page.tsx' 'apps/web/e2e/package-details.spec.ts'
          git commit -m 'VS-36 format frontend files'
          git push origin HEAD:agent/vs36-package-details-design-correction
