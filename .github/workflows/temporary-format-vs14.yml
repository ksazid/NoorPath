name: Temporary VS-14 Format Fix

on:
  push:
    branches:
      - codex/vs-14-operational-support

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/vs-14-operational-support
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - name: Apply pinned Prettier
        run: pnpm --dir apps/web exec prettier --write app/operator/support/page.tsx e2e/operational-support.spec.ts
      - name: Commit formatter output
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/app/operator/support/page.tsx apps/web/e2e/operational-support.spec.ts
          git commit -m "Apply pinned Prettier to VS-14 files"
          git push origin HEAD:codex/vs-14-operational-support
