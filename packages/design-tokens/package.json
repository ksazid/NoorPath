{
  "name": "@noorpath/design-tokens",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./tokens.css": "./tokens.css",
    "./tokens.json": "./tokens.json"
  },
  "scripts": {
    "build": "node -e \"JSON.parse(require('fs').readFileSync('tokens.json','utf8'))\"",
    "check": "node --test tokens.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test tokens.test.mjs"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
